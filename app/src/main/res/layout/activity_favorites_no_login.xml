<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <TextView
        android:id="@+id/favorites_no_login"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:text="@string/favorites_user_not_logged"
        android:textColor="@color/favorites_no_login_text_color"
        android:textSize="@dimen/favorites_no_login_text_size"
        android:gravity="center"
        android:layout_gravity="center"
        android:layout_margin="@dimen/favorites_no_login_text_margin"/>

</LinearLayout>